<% if @feed_items.any? %>
	<ol>
		<%= render partial: 'shared/feed_item', collection: @feed_items %>
	</ol>
    <%= will_paginate @feed_items, previous_label: 'Anterior', next_label: 'Próximo' %>
<% else %>
    <p>Sem novidades por enquanto...</p>
<% end %>
